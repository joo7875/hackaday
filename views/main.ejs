<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title></title>

    <style>
        .content {
            margin-left: 1em;
            margin-right: 1em;
            margin-top: 10em;
            margin-bottom: 1em;
        }
        .grid-container {
            max-width: 1200px;
            margin: auto;
            width: 100%; 
            display: grid;
            grid-auto-rows: 380px; 
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); 
            grid-gap: 1em;
        }
        .item {
            border-radius: 0px;
            padding: 0px;
            color: #fff;
            background-color: #e0e0e0;
            /*position: relative;
            overflow: hidden;*/
        }
        .image {
            height: 100%;
            width: 100%;
            object-fit: cover;
        }
    </style>

</head>

<body>

    <div class="content">
        <div class="grid-container">
            <% for (var i = 0; i < (apiData.projects).length; i++) { %>
                <% if (((apiData.projects[i]).image_url) != null) { %>
                <div class="item"><img class="image" id="img" src="<%= (apiData.projects[i]).image_url %>" /></div>
                <% } else { %>
                <div class="item"></div>
                <% } %>
            <% } %>
        </div>
    </div>


</body>
</html>