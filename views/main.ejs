<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title></title>

    <style>
        .content {
            margin-left: auto;
            margin-right: auto;
            margin-top: 10em;
            margin-bottom: 1em;
        }
        .grid-container {
            max-width: 1200px;
            margin: auto;
            width: 100%; 
            display: grid;
            grid-auto-rows: 300px; 
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); 
            grid-gap: 1em;
        }
        .item {
            border-radius: 0px;
            padding: 0px;
            color: #fff;
            background-color: #55BBE9;
        }
        .image {
            width: 300px;
        }
    </style>
</head>

<body>

    <div class="content">
        <div class="grid-container">
            <% for (var i = 0; i < (apiData.projects).length; i++) { %>
            <div class="item"><img class="image" src="<%= (apiData.projects[i]).image_url %>" /></div>
            <% } %>
        </div>
    </div>


</body>
</html>
